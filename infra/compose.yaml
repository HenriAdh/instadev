name: instadev

services:
  db:
    image: postgres:18
    container_name: db_instadev
    env_file:
      - ../.env
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/docker

volumes:
  postgres_data:
